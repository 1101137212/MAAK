@model IEnumerable<MAAK.Models.Portfolio>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Portfolio_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Portfolio_Date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Portfolio_Picture)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Member.Member_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Portfolio_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Portfolio_Date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Portfolio_Picture)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Member.Member_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Portfolio_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Portfolio_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Portfolio_ID })
        </td>
    </tr>
}

</table>
